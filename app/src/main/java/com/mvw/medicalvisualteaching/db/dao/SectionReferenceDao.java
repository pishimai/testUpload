package com.mvw.medicalvisualteaching.db.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.mvw.medicalvisualteaching.bean.offlinebook.SectionReference;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "Section_Reference".
*/
public class SectionReferenceDao extends AbstractDao<SectionReference, Void> {

    public static final String TABLENAME = "Section_Reference";

    /**
     * Properties of entity SectionReference.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "id", false, "ID");
        public final static Property LearningCenter_id = new Property(1, String.class, "learningCenter_id", false, "learningCenter_id");
    }


    public SectionReferenceDao(DaoConfig config) {
        super(config);
    }
    
    public SectionReferenceDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SectionReference entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String learningCenter_id = entity.getLearningCenter_id();
        if (learningCenter_id != null) {
            stmt.bindString(2, learningCenter_id);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SectionReference entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String learningCenter_id = entity.getLearningCenter_id();
        if (learningCenter_id != null) {
            stmt.bindString(2, learningCenter_id);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public SectionReference readEntity(Cursor cursor, int offset) {
        SectionReference entity = new SectionReference( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // learningCenter_id
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SectionReference entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setLearningCenter_id(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(SectionReference entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(SectionReference entity) {
        return null;
    }

    @Override
    public boolean hasKey(SectionReference entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
